<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:orientation="vertical">

    <!-- Field for user to input pick-up location -->
    <EditText
        android:id="@+id/location_editTo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/toHint" />

    <!-- Field for user to drop-off location -->
    <EditText
        android:id="@+id/location_editFrom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/fromHint" />

    <!--Date and Time selection for leaving location-->
    <Button
        android:id="@+id/selectDate_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/selectDate" />

    <Button
        android:id="@+id/selectTime_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/selectTime" />

    <!-- Set OnClickListener to trigger search when pressed -->
    <Button
        android:id="@+id/searchResultsList_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/search" />

    <!-- Set OnClickListener to go back to homepage when pressed -->
    <Button
        android:id="@+id/homepage_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/backToHomepage" />


</LinearLayout>
